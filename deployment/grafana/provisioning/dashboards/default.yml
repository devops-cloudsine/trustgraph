apiVersion: 1

providers:
  - name: 'TrustGraph Dashboards'
    orgId: 1
    folder: 'TrustGraph'
    folderUid: 'trustgraph'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
